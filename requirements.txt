streamlit
    google-generativeai>=0.5.0
    ```
  
  
    코드 중간에 모델을 정의하는 부분에 `models/`가 꼭 붙어 있어야 합니다.
    - **맞음:** `model_name="models/gemini-1.5-flash"`
    - **틀림:** `model_name="gemini-1.5-flash"`
  
  
    수정 사항을 GitHub에 저장(Commit)한 뒤, 스트림릿 화면 우측 하단의 **[Manage app] → [⋮] → [Reboot app]**을 반드시 누르세요. 
    > **주의:** 단순히 웹페이지 새로고침(F5)만으로는 해결되지 않습니다.
